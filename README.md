# Theme Squeeze downloads

Public app downloads and the Sparkle update feed for Theme Squeeze, a local macOS media preparation app.

The application source is maintained in a separate private repository. This repository contains distribution metadata and release downloads only.

The first public release is pending Developer ID signing and Apple notarization. No public app download is available yet.

Requires macOS 15 or later. Release binaries will support Apple silicon and Intel.
